"use client";

import { useEffect, useMemo, useState } from "react";
import { ChevronDown, ChevronLeft, ChevronRight, Download, Menu, RotateCcw, Search } from "lucide-react";
import { AppSidebar } from "../navigation/app-sidebar";
import { Card } from "../ui/card";
import { Button } from "../ui/button";
import { Badge } from "../ui/badge";
import { ChartCard } from "../design-system/chart-card";
import { ErrorState } from "../design-system/error-state";
import { KpiCard } from "../design-system/kpi-card";
import { LoadingSkeleton } from "../design-system/loading-skeleton";
import { PageHeader } from "../design-system/page-header";
import { TableCard } from "../design-system/table-card";
import { FilterBar } from "../design-system/filter-bar";
import { cn } from "../../lib/utils";
import { PRODUCT_GROUPS, productCategory } from "../../lib/dashboard/product-groups";

const MONTHS = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
const BRANCH_NAMES: Record<string, string> = { KMM01: "Hpa-an", KMM02: "Mawlamyine", KMM03: "Tharyarwaddy" };
type Key = "year" | "month" | "branch" | "salesperson" | "product" | "status";
type Filters = Record<Key, string[]>;
type Booking = { date: string; year: number | null; month: number | null; branch: string; salesperson: string; productType: string; model: string; price: number | null; bookingNo: string; customer: string; deposit: number | null; paymentType: string; financeType: string; statusDate: string; status: string };
type Data = { meta: { sourceUpdatedAt: string; sources: string[] }; booking: Booking[] };
const initial: Filters = { year: ["2026"], month: ["Jan", "Feb", "Mar", "Apr", "May", "Jun"], branch: [], salesperson: [], product: [], status: ["Open"] };

function safeNumber(value: unknown) {
  if (value === null || value === undefined || (typeof value === "string" && value.trim() === "")) return 0;
  const n = Number(value);
  return Number.isFinite(n) ? n : 0;
}
function isMissingNumber(value: unknown) {
  return value === null || value === undefined || (typeof value === "string" && value.trim() === "") || !Number.isFinite(Number(value));
}
function compact(value?: number | string | null) { return `${(safeNumber(value) / 1_000_000).toLocaleString("en-US", { maximumFractionDigits: 1 })}M`; }
function money(value?: number | string | null) { return isMissingNumber(value) ? "N/A" : safeNumber(value).toLocaleString("en-US", { maximumFractionDigits: 0 }); }
function priceOf(row: Booking) { return safeNumber(row.price); }
function isUnitProduct(row: Booking) { return (PRODUCT_GROUPS.UNIT_PRODUCTS as readonly string[]).includes(productCategory(row)); }
function isValueProduct(row: Booking) { return (PRODUCT_GROUPS.VALUE_PRODUCTS as readonly string[]).includes(productCategory(row)); }
function age(row: Booking) { const date = new Date(`${row.date}T00:00:00`); const today = new Date("2026-07-11T00:00:00"); const days = Math.floor((today.getTime() - date.getTime()) / 86_400_000); return Number.isFinite(days) ? Math.max(0, days) : 0; }
function risk(row: Booking) { const days = age(row); return days <= 30 ? "Healthy" : days <= 60 ? "Watch" : days <= 90 ? "At Risk" : "Critical"; }
function match(row: Booking, f: Filters) { return (!f.year.length || (row.year !== null && f.year.includes(String(row.year)))) && (!f.month.length || (row.month !== null && f.month.includes(MONTHS[row.month - 1]))) && (!f.branch.length || f.branch.includes(row.branch)) && (!f.salesperson.length || f.salesperson.includes(row.salesperson)) && (!f.product.length || f.product.includes(productCategory(row))) && (!f.status.length || f.status.includes(row.status)) && isValueProduct(row); }
function group(rows: Booking[], key: (row: Booking) => string) { const map = new Map<string, Booking[]>(); rows.forEach((row) => map.set(key(row), [...(map.get(key(row)) ?? []), row])); return [...map.entries()].map(([label, items]) => ({ label, rows: items, value: items.reduce((total, row) => total + priceOf(row), 0) })).sort((a, b) => b.rows.length - a.rows.length); }

function Select({ label, options, values, onChange }: { label: string; options: string[]; values: string[]; onChange: (value: string[]) => void }) {
  const [open, setOpen] = useState(false); const [query, setQuery] = useState(""); const visible = options.filter((option) => option.toLowerCase().includes(query.toLowerCase()));
  const display = values.length === 0 ? "All" : values.length === 1 ? values[0] : `${values.length} selected`;
  return <div className="relative min-w-0"><label className="mb-2 block text-[11px] font-bold uppercase tracking-[.12em] text-[#8A8E96]">{label}</label><button type="button" onClick={() => setOpen(!open)} className="flex h-11 w-full items-center justify-between rounded-xl border border-[#E5E7EB] bg-white px-3 text-sm font-semibold text-[#1F2937]"><span className="truncate">{display}</span><ChevronDown size={16} className="text-[#9CA3AF]"/></button>{open && <Card className="absolute left-0 right-0 top-[72px] z-50 p-2 shadow-xl"><div className="relative mb-2"><Search size={14} className="absolute left-3 top-1/2 -translate-y-1/2 text-[#9CA3AF]"/><input className="h-9 w-full rounded-lg border border-[#E5E7EB] bg-[#FAFBFC] pl-8 pr-2 text-sm outline-none" placeholder={`Search ${label.toLowerCase()}`} value={query} onChange={(e) => setQuery(e.target.value)}/></div><div className="max-h-52 space-y-1 overflow-auto">{visible.map((option) => <label key={option} className="flex cursor-pointer items-center gap-2 rounded-lg px-2 py-2 text-sm hover:bg-[#FFF7EF]"><input type="checkbox" checked={values.includes(option)} onChange={() => onChange(values.includes(option) ? values.filter((value) => value !== option) : [...values, option])} className="accent-[#FF8615]"/>{option}</label>)}</div></Card>}</div>;
}

function Bars({ items, value = (item: { rows: Booking[] }) => item.rows.length, suffix = " Units" }: { items: { label: string; rows: Booking[]; value: number }[]; value?: (item: { label: string; rows: Booking[]; value: number }) => number; suffix?: string }) { const measured = items.map((item) => ({ item, amount: safeNumber(value(item)) })); const max = Math.max(...measured.map(({ amount }) => amount), 1); return <div className="space-y-4">{measured.map(({ item, amount }) => <div key={item.label}><div className="mb-1.5 flex justify-between gap-3 text-xs"><span className="truncate font-medium text-[#4B5563]">{item.label}</span><span className="font-semibold text-[#1F2937]">{amount}{suffix}</span></div><div className="h-2 rounded-full bg-[#F1F2F4]"><div className="h-2 rounded-full bg-[#FF7A00]" style={{ width: `${(amount / max) * 100}%` }}/></div></div>)}</div>; }

function Trend({ rows }: { rows: Booking[] }) { const series = MONTHS.map((label, index) => ({ label, booking: rows.some((row) => row.month === index + 1) ? rows.filter((row) => row.month === index + 1 && row.status !== "Cancelled").length : null, delivered: rows.some((row) => row.month === index + 1) ? rows.filter((row) => row.month === index + 1 && row.status === "Delivered").length : null, cancelled: rows.some((row) => row.month === index + 1) ? rows.filter((row) => row.month === index + 1 && row.status === "Cancelled").length : null })); const max = Math.max(...series.flatMap((row) => [row.booking ?? 0, row.delivered ?? 0, row.cancelled ?? 0]), 1); const line = (key: "booking" | "delivered" | "cancelled") => series.map((row, i) => row[key] === null ? null : `${35 + i * 63},${185 - (row[key]! / max) * 145}`).reduce<string[]>((parts, point) => { if (point) { const last = parts.at(-1); if (last?.endsWith("|")) parts.push(point); else if (last) parts[parts.length - 1] = `${last} ${point}`; else parts.push(point); } else parts.push("|"); return parts; }, []).filter((part) => part !== "|"); return <div><div className="mb-4 flex flex-wrap gap-4 text-xs font-semibold"><span className="text-[#FF7A00]">● New Booking</span><span className="text-[#6B7280]">● Delivered</span><span className="text-[#DC2626]">● Cancelled</span></div><svg viewBox="0 0 760 220" className="h-[250px] w-full" role="img" aria-label="Booking trend"><path d="M35 185H730M35 112H730M35 40H730" stroke="#E5E7EB"/><polyline points={line("booking").join(" ")} fill="none" stroke="#FF7A00" strokeWidth="3"/><polyline points={line("delivered").join(" ")} fill="none" stroke="#6B7280" strokeWidth="3"/><polyline points={line("cancelled").join(" ")} fill="none" stroke="#DC2626" strokeWidth="3"/>{series.map((row, i) => <text key={row.label} x={35 + i * 63} y="210" textAnchor="middle" fontSize="11" fill="#6B7280">{row.label}</text>)}</svg><p className="text-xs text-[#9CA3AF]">Months without booking records are shown as gaps, not zero.</p></div>; }

export function BookingIntelligencePage() {
  const [data, setData] = useState<Data | null>(null), [loading, setLoading] = useState(true), [error, setError] = useState(""), [filters, setFilters] = useState<Filters>(initial), [collapsed, setCollapsed] = useState(false), [mobileOpen, setMobileOpen] = useState(false), [query, setQuery] = useState(""), [page, setPage] = useState(1);
  const load = (showLoading = true) => { if (showLoading) setLoading(true); setError(""); fetch("/dashboard-data.json").then((r) => r.ok ? r.json() : Promise.reject()).then((value) => setData(value)).catch(() => setError("Booking data could not be loaded.")).finally(() => setLoading(false)); }; useEffect(() => { const id = window.setTimeout(() => load(false), 0); return () => window.clearTimeout(id); }, []);
  const rows = useMemo(() => data?.booking.filter((row) => match(row, filters)) ?? [], [data, filters]);
  const openRows = rows.filter((row) => row.status === "Open"); const open = openRows.filter(isUnitProduct); const openValue = openRows.filter(isValueProduct); const options = useMemo(() => ({ year: [...new Set(data?.booking.map((r) => String(r.year)).filter((v) => v !== "null"))].sort().reverse(), month: MONTHS.filter((m, i) => data?.booking.some((r) => r.month === i + 1)), branch: [...new Set(data?.booking.map((r) => r.branch).filter(Boolean))].sort(), salesperson: [...new Set(data?.booking.map((r) => r.salesperson).filter(Boolean))].sort(), product: PRODUCT_GROUPS.UNIT_PRODUCTS as unknown as string[], status: [...new Set(data?.booking.map((r) => r.status).filter(Boolean))].sort() }), [data]);
  const health = ["Healthy", "Watch", "At Risk", "Critical"].map((label) => { const items = open.filter((row) => risk(row) === label); return { label, rows: items, value: items.reduce((total, row) => total + priceOf(row), 0) }; }); const product = PRODUCT_GROUPS.UNIT_PRODUCTS.map((label) => ({ label, rows: open.filter((r) => productCategory(r) === label), value: 0 })); const models = group(open, (r) => r.model || "Unknown model").slice(0, 10); const people = group(open, (r) => r.salesperson || "Unassigned").slice(0, 10); const payments = group(openValue, (r) => r.paymentType || "Unavailable"); const table = open.filter((row) => `${row.bookingNo} ${row.customer} ${row.date} ${row.branch} ${row.salesperson} ${row.model}`.toLowerCase().includes(query.toLowerCase())); const pages = Math.max(1, Math.ceil(table.length / 10)); const visible = table.slice((page - 1) * 10, page * 10);
  const update = (key: Key, value: string[]) => { setFilters((previous) => ({ ...previous, [key]: value })); setPage(1); }; const exportRows = () => { const headers = ["Booking Date", "Booking No.", "Customer", "Branch", "Salesperson", "Product", "Model", "Payment Type", "Booking Value (MMK)", "Deposit (MMK)", "Current Status", "Delivery / Cancel Date", "Booking Age (Days)", "Risk Status"]; const csv = [headers, ...table.map((r) => [r.date, r.bookingNo, r.customer, r.branch, r.salesperson, productCategory(r), r.model, r.paymentType, String(r.price ?? ""), String(r.deposit ?? ""), r.status, r.statusDate, String(age(r)), risk(r)])].map((row) => row.map((cell) => `"${String(cell ?? "").replaceAll('"', '""')}"`).join(",")).join("\n"); const url = URL.createObjectURL(new Blob([csv], { type: "text/csv" })); const link = document.createElement("a"); link.href = url; link.download = "kmm-booking-detail.csv"; link.click(); URL.revokeObjectURL(url); };
  return <div className="min-h-screen bg-[#F8FAFC] text-[#1F2937]"><AppSidebar collapsed={collapsed} mobileOpen={mobileOpen} onCollapsedChange={setCollapsed} onMobileOpenChange={setMobileOpen}/><div className={cn("transition-[padding] duration-300", collapsed ? "lg:pl-[76px]" : "lg:pl-[240px]")}><header className="sticky top-0 z-30 flex h-[82px] items-center gap-3 border-b border-[#E5E7EB] bg-white/95 px-4 backdrop-blur-md sm:px-6 xl:px-8"><button className="rounded-xl border border-[#E5E7EB] p-2.5 lg:hidden" onClick={() => setMobileOpen(true)} aria-label="Open navigation"><Menu size={19}/></button><div><h1 className="text-lg font-bold">Booking</h1><p className="text-xs text-[#9CA3AF]">KMM Sales Intelligence</p></div><div className="ml-auto"><Badge variant="outline" className="hidden gap-2 py-2 sm:inline-flex"><span className="size-2 rounded-full bg-[#22C55E]"/>Live data</Badge></div></header><main className="mx-auto max-w-[1600px] space-y-6 p-4 sm:p-6 xl:p-8"><section className="space-y-5"><PageHeader eyebrow="KUBOTA MAESOD MYANMAR (KMM)" title="Booking Intelligence" description={data ? `Open-booking pipeline and follow-up priorities. Refreshed ${data.meta?.sourceUpdatedAt ?? "N/A"}` : "Open-booking pipeline and follow-up priorities."}/><FilterBar actions={<><Button variant="outline" className="h-11" onClick={() => { setFilters(initial); setPage(1); }}><RotateCcw size={16}/>Reset</Button><Button className="h-11" onClick={exportRows}><Download size={16}/>Export</Button></>}><Select label="Year" options={options.year} values={filters.year} onChange={(v) => update("year", v)}/><Select label="Month" options={options.month} values={filters.month} onChange={(v) => update("month", v)}/><Select label="Branch" options={options.branch} values={filters.branch} onChange={(v) => update("branch", v)}/><Select label="Salesperson" options={options.salesperson} values={filters.salesperson} onChange={(v) => update("salesperson", v)}/><Select label="Product Type" options={options.product} values={filters.product} onChange={(v) => update("product", v)}/><Select label="Booking Status" options={options.status} values={filters.status} onChange={(v) => update("status", v)}/></FilterBar></section>{loading && <LoadingSkeleton variant="chart"/>}{error && <ErrorState message={error} onRetry={load}/>} {data && !loading && !error && <><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 xl:gap-3"><KpiCard title="Open Booking Unit" value={open.length} unit="Units" productBreakdown={<div className="mt-4 flex flex-wrap gap-2 text-[11px] font-semibold text-[#6B7280]">{PRODUCT_GROUPS.UNIT_PRODUCTS.map((p) => <span key={p}>{p} {open.filter((r) => productCategory(r) === p).length}</span>)}</div>}/><KpiCard title="Booking Value" value={compact(openValue.reduce((s, r) => s + priceOf(r), 0))} unit="MMK"/><KpiCard title="Deposit Received" value={compact(openValue.reduce((s, r) => s + safeNumber(r.deposit), 0))} unit="MMK"/><KpiCard title="Average Booking Age" value={open.length ? Math.round(open.reduce((s, r) => s + age(r), 0) / open.length) : "N/A"} unit={open.length ? "Days" : ""}/><KpiCard title="Booking Conversion Rate" value={rows.length ? `${((rows.filter((r) => r.status === "Delivered").length / rows.length) * 100).toFixed(1)}%` : "N/A"} unit="" supportingText="Booking → Delivered"/></section><ChartCard title="Booking Health Summary" subtitle="Open booking age as of 11 Jul 2026"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{health.map((item, i) => <div key={item.label} className={["border-[#DCFCE7] bg-[#F0FDF4]", "border-[#FEF3C7] bg-[#FFFBEB]", "border-[#FED7AA] bg-[#FFF7ED]", "border-[#FECACA] bg-[#FEF2F2]"][i] + " rounded-xl border p-4"}><p className={["text-[#15803D]", "text-[#B45309]", "text-[#C2410C]", "text-[#DC2626]"][i] + " text-sm font-bold"}>{["0–30 Days", "31–60 Days", "61–90 Days", ">90 Days"][i]} · {item.label}</p><p className="mt-3 text-2xl font-semibold">{item.rows.length} <span className="text-xs text-[#6B7280]">Units</span></p><p className="mt-2 text-xs text-[#4B5563]">{compact(item.value)} MMK · {open.length ? ((item.rows.length / open.length) * 100).toFixed(1) : "0.0"}%</p><p className="mt-1 text-xs text-[#9CA3AF]">Deposit: N/A</p></div>)}</div></ChartCard><ChartCard title="Booking Trend" subtitle="New booking, delivered and cancelled units by booking month" minHeight={360}><Trend rows={data.booking.filter((r) => match(r, { ...filters, status: [] })).filter(isUnitProduct)}/></ChartCard><section className="grid gap-5 xl:grid-cols-[1.1fr_1.9fr]"><ChartCard title="Booking Funnel" subtitle="Only stages present in the booking extract"><div className="space-y-3">{["Booking", "Deposit", "Finance Approved", "Ready for Delivery", "Delivered"].map((stage, i) => <div key={stage} className="grid grid-cols-[150px_1fr_64px] items-center gap-3 text-sm"><span className="font-medium">{stage}</span><div className="h-8 rounded bg-[#F1F2F4]"><div className="h-8 rounded bg-[#FF7A00]" style={{ width: i === 0 ? "100%" : i === 4 ? `${rows.length ? (rows.filter((r) => r.status === "Delivered").length / rows.length) * 100 : 0}%` : "0%" }}/></div><span className="text-right text-xs font-semibold">{i === 0 ? rows.length : i === 4 ? rows.filter((r) => r.status === "Delivered").length : "N/A"}</span></div>)}</div></ChartCard><div className="grid gap-4 md:grid-cols-3">{["KMM01", "KMM02", "KMM03"].map((branch) => { const items = open.filter((r) => r.branch === branch); const branchRows = rows.filter((r) => r.branch === branch); return <Card key={branch} className="p-5"><p className="text-lg font-semibold">{branch} <span className="text-sm text-[#9CA3AF]">{BRANCH_NAMES[branch]}</span></p><div className="mt-4 grid grid-cols-2 gap-4 text-sm"><div><p className="text-xs text-[#9CA3AF]">Open Booking</p><p className="mt-1 font-semibold">{items.length}</p></div><div><p className="text-xs text-[#9CA3AF]">Booking Value</p><p className="mt-1 font-semibold">{compact(openValue.filter((r) => r.branch === branch).reduce((s, r) => s + priceOf(r), 0))}</p></div><div><p className="text-xs text-[#9CA3AF]">Average Age</p><p className="mt-1 font-semibold">{items.length ? Math.round(items.reduce((s, r) => s + age(r), 0) / items.length) : "N/A"}</p></div><div><p className="text-xs text-[#9CA3AF]">Critical</p><p className="mt-1 font-semibold text-[#DC2626]">{items.filter((r) => risk(r) === "Critical").length}</p></div><div><p className="text-xs text-[#9CA3AF]">Deposit</p><p className="mt-1 font-semibold">{compact(items.reduce((s, r) => s + safeNumber(r.deposit), 0))}</p></div><div><p className="text-xs text-[#9CA3AF]">Conversion</p><p className="mt-1 font-semibold">{branchRows.length ? `${((branchRows.filter((r) => r.status === "Delivered").length / branchRows.length) * 100).toFixed(1)}%` : "N/A"}</p></div></div><div className="mt-5 flex items-end gap-1 border-t pt-3"><span className="h-8 flex-1 bg-[#FF7A00]" style={{ height: `${Math.max(10, items.length * 8)}px` }}/><span className="h-8 flex-1 bg-[#9CA3AF]" style={{ height: `${Math.max(10, branchRows.filter((r) => r.status === "Delivered").length * 8)}px` }}/></div></Card>; })}</div></section><section className="grid gap-5 xl:grid-cols-2"><ChartCard title="Booking by Product" subtitle="Open booking units"><Bars items={product}/></ChartCard><ChartCard title="Top 10 Model" subtitle="Open booking units"><Bars items={models}/></ChartCard><ChartCard title="Top 10 Salesperson" subtitle="Open booking and critical backlog"><div className="space-y-3">{people.map((item) => <div key={item.label} className="grid grid-cols-[minmax(0,1fr)_68px_68px] gap-2 text-xs"><span className="truncate font-medium">{item.label}</span><span className="text-right">{item.rows.length} open</span><span className="text-right text-[#DC2626]">{item.rows.filter((r) => age(r) > 60).length} &gt;60d</span></div>)}</div></ChartCard><ChartCard title="Booking by Payment / Finance Type" subtitle="Open booking value"><Bars items={payments}/></ChartCard></section><ChartCard title="Booking Aging Matrix" subtitle="Open booking unit by model and age"><div className="overflow-x-auto"><table className="min-w-[720px] w-full text-sm"><thead className="bg-[#FAFBFC] text-left text-xs text-[#6B7280]"><tr><th className="p-3">Model</th>{["0–30", "31–60", "61–90", ">90"].map((h) => <th key={h} className="p-3 text-center">{h}</th>)}<th className="p-3 text-center">Total</th></tr></thead><tbody>{group(open, (r) => r.model || "Unknown model").map((item) => <tr key={item.label} className="border-t border-[#EEF0F3]"><td className="p-3 font-medium">{item.label}</td>{["Healthy", "Watch", "At Risk", "Critical"].map((bucket, i) => { const count = item.rows.filter((r) => risk(r) === bucket).length; return <td key={bucket} className="p-3 text-center"><span className={["bg-[#DCFCE7]", "bg-[#FEF3C7]", "bg-[#FED7AA]", "bg-[#FECACA]"][i] + " inline-block min-w-10 rounded px-2 py-1"}>{count}</span></td>; })}<td className="p-3 text-center font-semibold">{item.rows.length}</td></tr>)}</tbody></table></div></ChartCard><ChartCard title="Management Follow-up" subtitle="Evidence-based recommendations from the active filter"><div className="space-y-3">{open.filter((r) => age(r) > 90).sort((a, b) => age(b) - age(a)).slice(0, 5).map((r) => <div key={`${r.date}-${r.model}-${r.salesperson}`} className="flex items-center justify-between gap-4 rounded-xl border border-[#FECACA] bg-[#FEF2F2] p-3 text-sm"><span><strong>{r.model || "Unknown model"}</strong> · {r.branch} · {r.salesperson}</span><span className="shrink-0 font-semibold text-[#DC2626]">{age(r)} days · Escalate</span></div>)}{!open.some((r) => age(r) > 90) && <p className="text-sm text-[#6B7280]">No open booking older than 90 days in the active filter.</p>}</div></ChartCard><TableCard title="Booking Detail" search={<div className="relative"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-[#9CA3AF]"/><input value={query} onChange={(e) => { setQuery(e.target.value); setPage(1); }} className="h-9 w-64 rounded-lg border border-[#E5E7EB] bg-[#FAFBFC] pl-9 pr-3 text-sm outline-none" placeholder="Search booking detail"/></div>} exportAction={<Button className="h-9" onClick={exportRows}><Download size={15}/>Export</Button>} pagination={<div className="flex items-center justify-between text-xs text-[#6B7280]"><span>{table.length} open booking(s)</span><div className="flex items-center gap-2"><button disabled={page === 1} onClick={() => setPage(page - 1)} className="rounded border p-1 disabled:opacity-40"><ChevronLeft size={15}/></button><span>{page} / {pages}</span><button disabled={page === pages} onClick={() => setPage(page + 1)} className="rounded border p-1 disabled:opacity-40"><ChevronRight size={15}/></button></div></div>} empty={!visible.length}><div className="max-h-[480px] overflow-auto rounded-xl border border-[#EEF0F3]"><table className="min-w-[1420px] w-full text-left text-xs"><thead className="sticky top-0 z-10 bg-[#FAFBFC] text-[#6B7280]"><tr>{["Booking Date", "Booking No.", "Customer", "Branch", "Salesperson", "Product", "Model", "Payment Type", "Booking Value", "Deposit", "Deposit %", "Booking Age", "Current Status", "Delivery / Cancel Date", "Risk Status"].map((h) => <th key={h} className="whitespace-nowrap px-3 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-[#F1F2F4]">{visible.map((r, i) => <tr key={`${r.date}-${r.model}-${i}`}><td className="whitespace-nowrap px-3 py-3">{r.date}</td><td className="px-3 py-3">{r.bookingNo}</td><td className="px-3 py-3">{r.customer || "N/A"}</td><td className="px-3 py-3">{r.branch}</td><td className="px-3 py-3">{r.salesperson}</td><td className="px-3 py-3">{productCategory(r)}</td><td className="px-3 py-3 font-semibold">{r.model}</td><td className="px-3 py-3">{r.paymentType || "N/A"}</td><td className="px-3 py-3 text-right">{money(r.price)}</td><td className="px-3 py-3 text-right">{money(r.deposit)}</td><td className="px-3 py-3 text-right">{!isMissingNumber(r.deposit) && !isMissingNumber(r.price) && safeNumber(r.price) !== 0 ? `${((safeNumber(r.deposit) / safeNumber(r.price)) * 100).toFixed(1)}%` : "N/A"}</td><td className="px-3 py-3 text-center">{age(r)}</td><td className="px-3 py-3">{r.status}</td><td className="px-3 py-3">{r.statusDate || "N/A"}</td><td className="px-3 py-3"><span className={risk(r) === "Critical" ? "font-semibold text-[#DC2626]" : risk(r) === "At Risk" ? "text-[#C2410C]" : "text-[#4B5563]"}>{risk(r)}</span></td></tr>)}</tbody></table></div></TableCard><p className="pb-2 text-xs text-[#9CA3AF]">Source: {(data.meta?.sources ?? []).join(" · ") || "N/A"} · Fields unavailable in the source are marked N/A.</p></>}</main></div></div>;
}
