"use client";

import { useState } from "react";
import { chartStroke, chartTheme } from "./chartTheme";

export type StandardLineSeries = { id: string; label: string; values: Array<number | null>; kind?: "current" | "previous" | "older" | "target" };
export type StandardLineChartProps = { labels: string[]; series: StandardLineSeries[]; unit?: string; height?: number; formatValue?: (value: number) => string };

function niceTicks(max: number, unit: string) { if (/unit/i.test(unit) && max <= 100) return [0, 20, 40, 60, 80, 100]; const rawStep = Math.max(max, 1) / 5; const magnitude = 10 ** Math.floor(Math.log10(rawStep)); const normalized = rawStep / magnitude; const nice = normalized <= 1 ? 1 : normalized <= 2 ? 2 : normalized <= 2.5 ? 2.5 : normalized <= 5 ? 5 : 10; const step = nice * magnitude; return [0, step, step * 2, step * 3, step * 4, step * 5]; }

export function StandardLineChart({ labels, series, unit = "Unit", height = 400, formatValue = (value) => value.toLocaleString() }: StandardLineChartProps) {
  const [hover, setHover] = useState<number | null>(null); const [open, setOpen] = useState(false); const [visibleIds, setVisibleIds] = useState<string[]>(() => series.map((item) => item.id)); const width = 900; const px = 70; const py = 36;
  const visibleSeries = series.filter((item) => visibleIds.includes(item.id));
  const max = Math.max(...visibleSeries.flatMap((item) => item.values.map((value) => value ?? 0)), 1); const ticks = niceTicks(max, unit); const top = ticks.at(-1) ?? 1;
  const x = (index: number) => px + (index * (width - px * 2)) / Math.max(labels.length - 1, 1); const y = (value: number) => height - py - (value / top) * (height - py * 2);
  const path = (values: Array<number | null>) => values.reduce((result, value, index) => { if (value === null) return result; const previous = values[index - 1]; if (previous === null || previous === undefined) return `${result} M ${x(index)} ${y(value)}`; const middle = (x(index - 1) + x(index)) / 2; return `${result} C ${middle} ${y(previous)}, ${middle} ${y(value)}, ${x(index)} ${y(value)}`; }, "");
  const style = (item: StandardLineSeries, index: number) => { const kind = item.kind ?? (index === 0 ? "current" : index === 1 ? "previous" : "older"); if (kind === "target") return { color: chartTheme.target, width: chartStroke.target, dash: "6 6" }; if (kind === "current") return { color: chartTheme.current, width: chartStroke.current }; if (kind === "previous") return { color: chartTheme.previous, width: chartStroke.previous }; return { color: chartTheme.older[Math.max(0, index - 2) % chartTheme.older.length], width: chartStroke.older }; };
  return <div className="relative"><div className="mb-3 flex items-start justify-between gap-3"><div className="flex flex-wrap gap-x-5 gap-y-2 text-xs font-semibold text-[#4B5563]">{visibleSeries.map((item, index) => { const token = style(item, index); return <span key={item.id} className="flex items-center gap-2"><i className="h-0 w-6 border-t-[3px]" style={{ borderColor: token.color, borderStyle: token.dash ? "dashed" : "solid" }} />{item.label}</span>; })}</div>{series.length > 1 && <div className="relative shrink-0"><button type="button" onClick={() => setOpen((value) => !value)} className="h-[38px] rounded-lg border border-[#E5E7EB] bg-white px-3 text-xs font-semibold text-[#1F2937]">Compare · {visibleSeries.length}</button>{open && <div className="absolute right-0 top-10 z-20 w-44 rounded-xl border border-[#E5E7EB] bg-white p-2 shadow-xl">{series.map((item) => <label key={item.id} className="flex cursor-pointer items-center gap-2 rounded-lg px-2 py-1.5 text-xs hover:bg-[#FFF7EF]"><input type="checkbox" checked={visibleIds.includes(item.id)} onChange={() => setVisibleIds((current) => current.includes(item.id) ? current.length > 1 ? current.filter((id) => id !== item.id) : current : [...current, item.id])} className="accent-[#F57C00]" />{item.label}</label>)}</div>}</div>}</div><svg className="w-full" style={{ height }} viewBox={`0 0 ${width} ${height}`} role="img" aria-label="Trend chart" onMouseLeave={() => setHover(null)}><title>Premium line chart</title>{ticks.map((tick) => <g key={tick}><line x1={px} x2={width - px} y1={y(tick)} y2={y(tick)} stroke={chartTheme.grid} /><text x={px - 10} y={y(tick) + 4} textAnchor="end" fontSize="11" fill={chartTheme.text}>{formatValue(tick)}</text></g>)}<text x={px} y={16} fontSize="11" fontWeight="700" fill={chartTheme.text}>{unit}</text>{visibleSeries.map((item, seriesIndex) => { const token = style(item, seriesIndex); return <g key={item.id}><path d={path(item.values)} fill="none" stroke={token.color} strokeWidth={token.width} strokeDasharray={token.dash} strokeLinecap="round" strokeLinejoin="round" style={{ transition: "stroke-dashoffset 800ms ease-in-out" }} />{item.values.map((value, index) => value === null ? null : <circle key={index} cx={x(index)} cy={y(value)} r={hover === index ? 6 : 3.8} fill="white" stroke={token.color} strokeWidth="2.2" onMouseEnter={() => setHover(index)} />)}</g>; })}{labels.map((label, index) => <text key={label} x={x(index)} y={height - 8} textAnchor="middle" fontSize="11" fontWeight="600" fill={chartTheme.text}>{label}</text>)}</svg>{hover !== null && <div className="pointer-events-none absolute top-12 z-10 min-w-44 rounded-lg border border-[#E5E7EB] bg-white px-3 py-2 text-xs text-[#4B5563] shadow-[0_10px_28px_rgba(31,41,55,.1)]" style={{ left: `${Math.min(Math.max((x(hover) / width) * 100, 8), 72)}%` }}><p className="font-bold text-[#1F2937]">{labels[hover]}</p>{visibleSeries.map((item) => <p key={item.id}>{item.label}: <strong>{item.values[hover] === null ? "No data" : formatValue(item.values[hover] ?? 0)}</strong></p>)}</div>}</div>;
}
